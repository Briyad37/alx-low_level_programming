#include<stdio.h>
/**
 * main -prints "Programming is like bulding a multilingual puzzle"
 *
 * Return: 0 on success, non-zero on failure
 */
int main(void)
{
	const char *alx = "\"Programming is like building a multilingual puzzle";

	puts(alx);
	return (0);
}
